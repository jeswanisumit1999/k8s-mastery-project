apiVersion: v1
kind: ResourceQuota
metadata:
  name: dev-quota
  namespace: dev
spec:
  hard:
    requests.cpu: "1"
    requests.memory: "800Mi"
    limits.cpu: "2"
    limits.memory: "1500Mi"
    pods: "10"

apiVersion: v1
kind: ResourceQuota
metadata:
  name: dev-quota
  namespace: dev
spec:
  hard:
    requests.cpu: "1"
    requests.memory: "1000Mi"
    limits.cpu: "2"
    limits.memory: "2000Mi"
    pods: "10"
